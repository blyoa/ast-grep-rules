id: no-for-tuple-arrays
snapshots:
  ? |
    test.for([[1, "one"], [2, "two"]], (input, expected) => {
      expect(formatNumber(input)).toBe(expected);
    });
  : labels:
    - source: |-
        test.for([[1, "one"], [2, "two"]], (input, expected) => {
          expect(formatNumber(input)).toBe(expected);
        })
      style: primary
      start: 0
      end: 106
